uid: ems_analyse
tags:
- emstab
props:
  parameters: []
timestamp: Jun 1, 2026, 7:40:00 AM
component: oh-layout-page
config:
  label: Energy - Analysis
  sidebar: false
slots:
  default:
  - component: oh-block
    config: {}
    slots:
      default:
      - component: oh-grid-row
        config:
          gap: true
        slots:
          default:
          - component: oh-grid-col
            config:
              width: '100'
            slots:
              default:
              - component: widget:ems_analyse_tab
                config: {}
  - component: oh-block
    config: {}
    slots:
      default:
      - component: oh-grid-row
        config:
          gap: true
        slots:
          default:
          - component: oh-grid-col
            config:
              width: '100'
            slots:
              default:
              - component: widget:emsBatterySweepChart
                config: {}
  - component: oh-block
    config: {}
    slots:
      default:
      - component: oh-grid-row
        config:
          gap: true
        slots:
          default:
          - component: oh-grid-col
            config:
              width: '100'
            slots:
              default:
              - component: widget:emsTariffRankingChart
                config: {}
